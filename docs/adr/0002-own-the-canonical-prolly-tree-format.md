# Own the Canonical Prolly Tree Format

Pactium will implement its own protocol-complete canonical Prolly Tree instead of placing a third-party npm Prolly package in the trusted proof path. The tree's encoding, content-defined chunking, hash domains, and proof format become receipt semantics, so Pactium must control them directly and specify the full verification model from the first verifiable-state release.
