# Own the Canonical Prolly Tree Format

Pactium implements its own protocol-complete canonical Prolly Tree instead of placing a third-party npm Prolly package in the trusted proof path. The tree's encoding, content-defined chunking, hash domains, and proof format are receipt semantics, so Pactium controls them directly and specifies the full verification model in the active protocol profile.
