# Root Export Latest Proof API Only

The Pactium package root export exposes only the latest proof-first API. Earlier experimental storage-first or host-specific APIs do not remain, because mixed surfaces would let callers depend on weak or repository-external semantics accidentally.
