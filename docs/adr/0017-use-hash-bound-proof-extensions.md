# Use Hash-Bound Proof Extensions

Pactium Proof Envelopes will have a fixed core schema plus hash-bound extensions for host-specific evidence such as LicoLite policy material. Extensions may be critical or non-critical, but they do not merge arbitrary host fields into the core proof schema.
