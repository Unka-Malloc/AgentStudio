# Use Hash-Bound Proof Extensions

Pactium Proof Envelopes have a fixed core schema plus hash-bound extensions for caller-supplied host evidence or explicit portable content copies. Extensions may be critical or non-critical, but they do not merge arbitrary host fields into the core fact schema. Pactium verifies their integrity and required verifier support, not their business truth or disclosure safety.
