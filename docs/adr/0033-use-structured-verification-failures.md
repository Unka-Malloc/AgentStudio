# Use Structured Verification Failures

Pactium verifiers, including the LicoLite Verifier, return structured verification failures with layer, code, severity, and evidence references instead of a single boolean or generic invalid result. LicoLite needs this classification for UI, operations, and repair workflows.
