# Use Structured Verification Failures

Pactium verifiers return structured verification failures with layer, code, severity, and evidence references instead of a single boolean or generic invalid result. Hosts use this classification for diagnostics, operations, and repair planning.
