# Provide a LicoLite-Level Verifier

The LicoLite Aspect will provide a LicoLite-level verifier rather than only a writer adapter. It verifies core Pactium proofs plus LicoLite signing, workspace projection, policy extension, and workspace effect extension requirements, so LicoLite does not need to duplicate Pactium proof verification logic.
