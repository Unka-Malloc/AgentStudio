# Bind Workspace Projections to the Global Ledger

Workspace Projection is a verifiable projection index whose updates are bound by global Operation Ledger entries, not a simple UI filter over ledger rows. This keeps the global Ledger as ordering authority while allowing workspace-scoped order and membership proofs. Projection membership is not tenant, authorization, or storage isolation.
