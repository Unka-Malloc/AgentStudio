# Bind Workspace Projections to the Global Ledger

Workspace Projection will be a verifiable projection index whose updates are bound by global Operation Ledger entries, not a simple UI filter over ledger rows. This keeps the global Ledger as ordering authority while allowing workspace-scoped proofs and views for LicoLite isolation and presentation needs.
