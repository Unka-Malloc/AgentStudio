# Use a Proof-First Public API

Pactium will expose a proof-first public API and remove or hide current weak proof surfaces such as proof hashes, loose receipt identifiers, and storage-shaped records as stable outputs. The public API should return verifiable proof envelopes, heads, and verification results because Pactium's value is trust semantics rather than raw persistence access.
