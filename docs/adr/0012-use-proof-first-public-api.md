# Use a Proof-First Public API

Pactium exposes a proof-first public API and removes or hides weak proof surfaces such as proof hashes, loose receipt identifiers, and storage-shaped records as stable outputs. The public API returns verifiable proof envelopes, heads, bundles, and verification results because Pactium's value is trust semantics rather than raw persistence access.
