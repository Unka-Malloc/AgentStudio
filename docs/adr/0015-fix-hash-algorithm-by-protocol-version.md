# Fix Hash Algorithm by Protocol Version

Pactium fixes the hash algorithm as a protocol constant rather than allowing hosts to configure it. Hashes define roots, proofs, and receipts, so changing the algorithm is a protocol-version change rather than a runtime option.
