services:
  pactium:
    build: .
    image: pactium:local
    container_name: pactium
    ports:
      - "7288:7288"
    environment:
      PACTIUM_DATA_DIR: /data
      PACTIUM_HTTP_PORT: "7288"
    volumes:
      - pactium-data:/data

volumes:
  pactium-data:
