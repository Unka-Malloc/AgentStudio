# AgentLibrary Playbook Framework

Human-maintainable framework bundle for Pact AgentLibrary Playbook creation.
Runtime reads `framework.json` from this bundle first and falls back to the
legacy `skills/knowledge-skill-framework` bundle during migration. This is a
built-in knowledge module framework, not a Skill Hub contributed skill asset.
