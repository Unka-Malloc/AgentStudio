# AgentLibrary Retrieval Playbook

Built-in AgentLibrary retrieval playbook for evidence-grounded knowledge-base
operations. This bundle ships with the knowledge module and is not a Skill Hub
contributed skill asset. Tools and protocol dependencies are declared in
`dependencies.json`; no large evidence payloads are bundled here.
